# API Endpoints Package (`app/api/`)

This package contains APIRouter definitions organized by domain:
- Router endpoints for auth, user management, resume processing, mock interviews, roadmaps, job discovery, and AI chat.
